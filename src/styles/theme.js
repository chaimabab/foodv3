import { createTheme } from '@rneui/themed';

const theme = createTheme({
    lightColors: {
        rouge: '#432C7A',
    },
    darkColors: {
        rouge: '#000',
    },
    mode: 'light',
});


export default theme;